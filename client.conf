# SecureCloudFS Client Configuration
# This file configures the client to use the production SecureCloudFS service

# Production API Endpoint (No need to change this)
API_BASE_URL=https://web-production-916e9.up.railway.app/api

# Service Information
SERVICE_NAME=SecureCloudFS
SERVICE_VERSION=1.0.0
SERVICE_PROVIDER=Jozef Hernandez

# Client Settings
DEFAULT_SYNC_INTERVAL=5
MAX_FILE_SIZE_MB=100
SUPPORTED_FILE_TYPES=*

# Logging
LOG_LEVEL=INFO
LOG_FILE=securecloud.log
