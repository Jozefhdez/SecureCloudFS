# SecureCloudFS Client Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies for SecureCloudFS client
cryptography==43.0.1
oci==2.155.0
watchdog==6.0.0
python-dotenv==1.0.0
requests==2.31.0
supabase==2.14.0
