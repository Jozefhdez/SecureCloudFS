# SecureCloudFS Backend Dependencies
# For Railway deployment only - client installs its own dependencies

# Core dependencies
flask>=2.3.0
flask-cors>=4.0.0
supabase>=2.14.0
oci>=2.155.0
cryptography>=43.0.0
python-dotenv>=1.0.0
