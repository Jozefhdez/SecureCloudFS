# SecureCloudFS Client Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies for file encryption and API communication
cryptography>=41.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# File watching for automatic synchronization
watchdog>=3.0.0

# Oracle Cloud SDK (for internal encryption/decryption)
oci>=2.100.0

# Supabase client for authentication
supabase>=1.0.0
